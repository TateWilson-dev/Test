version: 2
updates:
  # Keep the GitHub Actions used by this repo's workflows up to date.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    labels:
      - "dependencies"
